{
  "name": "aldengillespy-contact-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Contact-form handler (Turnstile verify + Cloudflare Email Sending) for aldengillespy.com v6. The only server-side compute in the stack; Pages stays fully static.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "types": "wrangler types"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "wrangler": "^4.113.0"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^5.20260721.1"
  }
}
