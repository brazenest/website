# Local environment example
# Copy to .env.development or .env.production and replace placeholder values.

# Public origin used by server-side rendering and request validation.
ORIGIN=http://localhost:3000

# PostgreSQL connection string for server-side blog content queries.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/website

# Contact form email delivery through Amazon SES.
AWS_REGION=us-east-1
CONTACT_FORM_FROM_EMAIL=no-reply@example.com
CONTACT_FORM_TO_EMAIL=you@example.com
CONTACT_FORM_SUBJECT_PREFIX=Website contact

# Optional for local testing when you are not using an AWS profile or role.
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_SESSION_TOKEN=

# Optional PostgreSQL SSL behavior. Uncomment when your database requires TLS.
# PGSSLMODE=require
# PGSSLREJECTUNAUTHORIZED=true

# Local server defaults. The EC2 deploy script passes these explicitly in production.
PORT=3000
HOST=0.0.0.0
NODE_ENV=development
